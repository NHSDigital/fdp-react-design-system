import { default as React } from 'react';
import { ButtonProps } from '../Button/Button.types';
/**
 * NHS Button Component (CSS-based)
 *
 * Uses pre-compiled CSS classes instead of runtime styling.
 * This version demonstrates the build-time token compilation approach.
 */
export declare const ButtonCSS: React.FC<ButtonProps>;
export default ButtonCSS;
