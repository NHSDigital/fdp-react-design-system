/**
 * Transitional export for historical consumers.
 * @deprecated Prefer using SPC engine enums and the Icons namespace, e.g.:
 *   import { SpcVisualCategory } from '../engine';
 *   import * as Icons from '../icons';
 * This symbol may be removed in a future major version.
 */
export { AssuranceIcon } from "./logic/spcConstants";
/**
 * Transitional export for historical consumers.
 * @deprecated Prefer using consumer-facing descriptors or engine metadata where available.
 * This symbol may be removed in a future major version.
 */
export { BaselineSuggestionReason } from "./logic/spcConstants";
/**
 * Transitional export for rule metadata.
 * @deprecated Prefer the descriptor helpers from the SPC barrel, for example:
 *   import { ruleGlossary, extractRuleIds } from '../SPCChart/descriptors';
 * This symbol may be removed in a future major version.
 */
export { RULE_METADATA } from "./logic/spcConstants";
