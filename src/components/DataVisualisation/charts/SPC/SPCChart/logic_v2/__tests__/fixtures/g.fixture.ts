// Geometric-style counts between events (0-based support). Includes multiple zeros
// to ensure lower bands remain non-negative by construction.
export const G_EXAMPLE_SERIES: number[] = [
  0, 1, 0, 2, 3, 1, 0, 4, 2, 1, 0, 3, 2, 0, 1, 5
];
