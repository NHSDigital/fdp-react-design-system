import { describe, it, expect } from 'vitest';

// Legacy combined Details tests have been split into:
//  - Details.ssr.test.tsx
//  - Details.client.test.tsx
//  - Details.hydration.test.tsx
// This placeholder remains temporarily to avoid merge conflicts; safe to delete once branches are aligned.
describe('Details (legacy placeholder)', () => {
  it('suite migrated to split files', () => {
    expect(true).toBe(true);
  });
});
