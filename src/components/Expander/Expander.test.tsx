import { describe, it, expect } from 'vitest';
// Legacy combined Expander tests have been split:
//  - Expander.ssr.test.tsx
//  - Expander.client.test.tsx
//  - Expander.hydration.test.tsx
// Safe to delete once branches align.
describe('Expander (legacy placeholder)', () => {
  it('suite migrated to split files', () => {
    expect(true).toBe(true);
  });
});
