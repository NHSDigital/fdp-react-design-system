import { describe, it, expect } from 'vitest';

// Legacy mixed Tabs tests have been migrated into separate SSR/client/hydration files.
// This placeholder remains temporarily to avoid churn across branches. Safe to delete later.

describe('Tabs legacy placeholder', () => {
  it('is migrated', () => {
    expect(true).toBe(true);
  });
});
