// Deprecated legacy macro parity harness for Input.
// The up-to-date tests live in `input.macroParity.test.tsx` using the shared parityHarness.
// This file is intentionally skipped to avoid duplicate execution and React controlled/uncontrolled warnings.
import { describe } from 'vitest';

describe.skip('Input macro parity (legacy harness deprecated)', () => {
  // All scenarios migrated.
});
